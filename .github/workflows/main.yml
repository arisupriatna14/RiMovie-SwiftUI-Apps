name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build iOS App (iOS, xcode12.3)
      run: xcodebuild -project RiMovie.xcodeproj -scheme "RiMovie (iOS)" -destination "platform=iOS Simulator,name=iPhone 12 Pro Max"
    - name: Build AppClip (iOS, xcode12.3)
      run: xcodebuild -project RiMovie.xcodeproj -scheme RiMovieAppClip -destination "platform=iOS Simulator,name=iPhone 12 Pro Max"
    - name: Build iOS App (iOS, xcode12.3)
      run: xcodebuild -project RiMovie.xcodeproj -scheme RiMovieWidgetExtension -destination "platform=iOS Simulator,name=iPhone 12 Pro Max"
